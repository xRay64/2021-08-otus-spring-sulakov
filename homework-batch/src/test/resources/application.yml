logging:
  level:
    root: INFO

spring:
  data:
    mongodb:
      port: 0
      database: test
      host: localhost

  mongodb:
    embedded:
      version: 3.4.0

  main:
    allow-circular-references: true

  shell:
    interactive:
      enabled: false